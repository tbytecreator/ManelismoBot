FROM rust:1.75

WORKDIR /app

# Copiar arquivos de cargo
COPY backend/Cargo.toml ./Cargo.toml
COPY backend/src ./src

# Build em release mode
RUN cargo build --release

# Expor porta
EXPOSE 8080

# Executar a aplicação
CMD ["./target/release/manelismo_bot_backend"]
